Place your app screenshot / phone mockup here.

The hero section looks for:  assets/phone-mockup.png

- Recommended: a portrait screenshot of the Manzar app UI (roughly 1080x2280 or any
  9:19.5-ish ratio). It will be shown inside the phone frame.
- If no image is present, the site gracefully shows a styled placeholder phone frame,
  so the page still looks complete without it.
- To use a different filename, update the <img src="assets/phone-mockup.png"> line in index.html.
